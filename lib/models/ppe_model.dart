import 'package:flutter/material.dart';

class PpeModel {
  int id;
  String label;
  Widget icon;

  // constructor
  PpeModel({
    required this.id,
    required this.label,
    required this.icon,
  });
}
