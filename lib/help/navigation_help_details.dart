import 'package:flutter/material.dart';

class NavigationHelpDetails extends StatelessWidget {
  const NavigationHelpDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
